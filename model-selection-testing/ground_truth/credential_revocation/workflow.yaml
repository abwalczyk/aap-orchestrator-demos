# Update these IDs to match your actual AO workflow and node IDs
workflow_id: "REPLACE_WITH_WORKFLOW_UUID"

steps:
  - name: evaluate
    node_id: "REPLACE_WITH_EVALUATE_NODE_ID"
    ground_truth_file: step1_evaluate.json
    description: Evaluate compromise severity and blast radius

  - name: respond
    node_id: "REPLACE_WITH_RESPOND_NODE_ID"
    ground_truth_file: step2_respond.json
    description: Determine revocation workflow and urgency
    depends_on: evaluate
